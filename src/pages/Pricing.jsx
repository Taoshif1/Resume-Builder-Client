import { useState } from "react";
import { useNavigate } from "react-router";
import { PLANS, PUBLIC_PLAN_FEATURES } from "../product/plans";
const cards = [{ id: "free", cta: "Start Free", className: "bg-[#EFECE3] text-black" }, { id: "pro", cta: "Upgrade to Pro", badge: "Most Popular", className: "bg-[#4A70A9] text-white" }, { id: "premium", cta: "Go Premium", badge: "Best Value", className: "bg-[#172033] text-white" }];
export default function Pricing() { const [yearly, setYearly] = useState(false); const navigate = useNavigate(); return <main className="py-20 px-6 bg-[#EFECE3]/30"><header className="text-center max-w-2xl mx-auto mb-10"><h1 className="text-5xl font-black tracking-tighter">Simple, transparent <span className="glow">pricing</span></h1><p className="mt-5 text-gray-600">Choose the tools that fit your job search. Upgrade checkout is not live yet; paid access remains server-controlled.</p><label className="inline-flex items-center gap-3 mt-6 font-bold">Monthly<input type="checkbox" className="toggle toggle-primary" checked={yearly} onChange={(event) => setYearly(event.target.checked)} />Yearly <span className="badge badge-success">Save 17%</span></label></header><section className="grid max-w-6xl mx-auto gap-6 md:grid-cols-3" aria-label="PersonaCV plans">{cards.map((card) => { const plan = PLANS[card.id]; const price = plan.price[yearly ? "yearly" : "monthly"]; return <article key={card.id} className={"relative rounded-[2rem] p-8 shadow-xl flex flex-col " + card.className}>{card.badge && <span className="absolute -top-3 left-8 rounded-full bg-yellow-300 text-black px-3 py-1 text-xs font-black uppercase">{card.badge}</span>}<h2 className="text-2xl font-black">{plan.label}</h2><p className="mt-4 text-4xl font-black">$${price}<span className="text-base font-medium opacity-70">/{yearly ? "yr" : "mo"}</span></p>{yearly && price > 0 && <p className="text-sm mt-1 opacity-80">Billed annually</p>}<ul className="mt-7 space-y-3 flex-1">{PUBLIC_PLAN_FEATURES[card.id].map((feature) => <li key={feature}>- {feature}</li>)}</ul><button className={"mt-8 w-full rounded-2xl py-4 font-black " + (card.id === "free" ? "bg-black text-white" : "bg-white text-black")} onClick={() => navigate(card.id === "free" ? "/get-started/register" : "/dashboard/settings")}>{card.cta}</button></article>; })}</section></main>; }