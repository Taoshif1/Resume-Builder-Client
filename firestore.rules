rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the authenticated API. Admin SDK access uses IAM.
    match /{document=**} { allow read, write: if false; }
  }
}
